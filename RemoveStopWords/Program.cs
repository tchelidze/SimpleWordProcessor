namespace RemoveStopWords
{
    public class Program
    {
        static void Main()
        {
            /*
                        var input = Console.ReadLine();
                        var stopWordsRemoved = input.RemoveStopwords();
                        var uniqueWords = stopWordsRemoved.DiscardSpacesAndGetUnique();
                        var georgianWords = uniqueWords.Where(GeorgianWord.IsGeorgianWord);

                        foreach (var uniqueWord in georgianWords)
                            Console.WriteLine(uniqueWord);
            */
        }
    }
}